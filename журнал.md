# Журнал работ

## 19.08.2026 — старт проекта

- **Что:** создал папку `E:\gesha-photo-site`, `git init`, README, .gitignore, память проекта.
  **Зачем:** по правилу «новый проект — первым делом репозиторий и память».
  **Результат:** каркас проекта готов, оригиналы фото исключены из git.
- **Что:** разобрал прототип — Framer-шаблон «Objective» через браузер.
  **Зачем:** понять структуру, прежде чем верстать.
  **Результат:** главная = меню поверх фона, разделы = сетка-галерея, у каждого фото своя страница.
  Решено заменить отдельные страницы фото на лайтбокс — быстрее и лучше на мобильном.
- **Что:** написал `_tools/download.py`, скачал 37 фото из публичной папки Яндекс.Диска.
  **Зачем:** контент для галерей.
  **Результат:** оригиналы в `_raw/{lovestory,family,personal}/`.

- **Что:** сверстал главную (меню-табличка 01–06 + смена фонового фото при наведении),
  три галереи, цены, о себе, контакты.
  **Зачем:** основной каркас сайта.
  **Результат:** все страницы открываются, консоль чистая, горизонтального скролла нет
  ни на 1280px, ни на 375px.
- **Что:** заменил CSS-columns в галерее на колонки, которые раскладывает JS
  (кадр уходит в самую короткую колонку).
  **Зачем:** ровный низ галереи и предсказуемая раскладка на любых пропорциях фото.
  **Результат:** 3 колонки на десктопе, 2 на планшете, 1 на телефоне.
- **Что:** скачал 37 фото (318 МБ), сжал в WebP двух размеров — 1100px для сетки,
  2000px для лайтбокса.
  **Зачем:** оригиналы по 3–14 МБ грузились бы минутами.
  **Результат:** папка `images/` весит 16,6 МБ вместо 318 МБ.
- **Что:** проверил лайтбокс скриптом в браузере.
  **Зачем:** убедиться, что работает не только на глаз.
  **Результат:** открытие, счётчик «1 / 12», стрелки с зацикливанием, Escape,
  блокировка прокрутки под оверлеем — всё отрабатывает.

**Не сделано (жду данных от Евгении):** текст «О себе» — сейчас черновой, написан по смыслу
из прайса; контакты кроме Instagram (телефон, Telegram, почта) — не заполнены.

## 19.08.2026 — переделка под прототип

- **Что:** развернул оформление под шаблон-прототип: белый фон, шапка «имя · раздел» + бургер,
  список разделов по центру, поле 50px, зазор 20px, 4 колонки, ч/б фото с возвратом цвета.
  **Зачем:** заказчик попросил «как на рефе».
  **Результат:** метрики сняты прямо с живого демо (шрифт Asap Condensed 34/500, ls 1.36px,
  поле 25px, зазор 16px). Asap Condensed без кириллицы — взят Roboto Condensed.
- **Что:** убрал страницы «О себе» и «Контакты», Instagram стал ссылкой в меню и подвале.
  **Зачем:** заказчик выбрал минимализм.
  **Результат:** осталось 5 страниц, тексты только со скриншотов заказчика.
- **Что:** прайс переделан по типографической шкале — крупная цена, маркеры, тонкие линии.
  **Зачем:** читаемость.
  **Результат:** проверено на 375 / 768 / 1024 / 1440 — горизонтального скролла нет.
- **Что:** `_tools/layout.py` — считает тон, контраст и «крупность плана» (по доле кожи в кадре)
  и переставляет кадры так, чтобы соседние различались по тону, плану и форме.
  **Зачем:** сетка должна читаться как композиция, а не как алфавитный список файлов.
  **Результат:** ритм виден по строкам, колонки заполняются по кругу с балансировкой высоты.

**Важно:** после каждого запуска `optimize.py` манифест пересоздаётся — сразу за ним нужно
запускать `layout.py`, иначе порядок кадров вернётся к алфавитному.
- **Что:** опубликовал сайт: репозиторий `shmelevaee-arch/gesha-photo-site` (публичный), GitHub Pages с ветки `main`.
  **Зачем:** заказчику нужен живой адрес.
  **Результат:** https://shmelevaee-arch.github.io/gesha-photo-site/ — все страницы и картинки отдают 200.
- **Что:** собрал четыре варианта главной (полноэкранный кадр, разворот, контактный лист,
  список с лентой), выложил превью на Pages, заказчик выбрал «список сверху, лента снизу».
  **Зачем:** прежняя главная выглядела перекошенной — текст в углу и пустой белый лист.
  **Результат:** главная = `index.html` + `assets/css/home.css`, помещается в один экран,
  превью-файлы удалены.
